name: Build and publish knowledge graph

on:
  workflow_dispatch:
  push:
    branches: [ "develop" ]

jobs:
  build-and-export:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t knowledgegraph .

      - name: Run container to generate graph
        run: |
          docker run --rm \
            -v ${{ github.workspace }}/workflow-output:/app/output \
            knowledgegraph \
            --config max-workflow-id=10 \
            --config output-graph="workflowhub-graph.trig" \
            --config output-dir=output

      - name: Upload TRIG artifact
        uses: actions/upload-artifact@v4
        with:
          name: "workflowhub-graph.trig"
          path: ${{ github.workspace }}/workflow-output/output/workflowhub-graph.trig
